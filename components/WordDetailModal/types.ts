export enum WordStatusType {
  NEW = 'New',
  LEARNING = 'Learning',
  MASTERED = 'Mastered',
  OVERDUE = 'Overdue',
  ON_TRACK = 'On track',
  NOT_STARTED = 'Not started',
}

export enum WordStatusMessage {
  REVIEW_NEEDED = 'Review needed',
  DUE_FOR_REVIEW = 'Due for review',
}

// Keep Edge Function imports stable while the source of truth lives in the
// shared domain workspace used by both mobile and web applications.
export * from '../../../packages/domain/src/app-config.ts'

# @woordenaar/content

Versioned learning content shared by the Expo and Next.js applications.

The package currently exports the official Dutch A1 starter-pack manifest as
JSON. Consumers must validate the manifest before converting it into platform
or database-specific word records.
